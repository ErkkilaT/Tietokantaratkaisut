spring:
  application:
    name: webstoreapi
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        ddl-auto: validate
  datasource:
    url: jdbc:mariadb://localhost:3306/firma
    username: dbuser
    password: viljo123